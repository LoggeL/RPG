/**
 * UI Manager - coordinates panel states.
 * Currently panels handle their own open/close via events.
 * This module exists as an extension point for future screen states.
 */
export const UIManager = {
  init() {
    // Extension point for splash screen, game over, etc.
  },
};
